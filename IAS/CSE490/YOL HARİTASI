tüm sectionlarda dön
	eğer instructor ismi ile = request i yapan kişinin adı aynı ise
		dersin section ı kaydet (X)

tüm taken courselarda dön
	eğer act course ile X aynı ise
		studentı, students list e ekle 


sections = Section.objects.all()
specificSection = ""
for section in sections:
	if section.instructor.user.first_name == request.user.first_name AND section.instructor.user.last_name == request.user.last_name:
		specificSection = section


takenCourses = TakenCourses.objects.all()
students = []
for takenCourse in takenCourses:
	if act_course == specificSection:
		for student in act_course:
			students += student